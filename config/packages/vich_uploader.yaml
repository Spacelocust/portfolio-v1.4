vich_uploader:
    db_driver: orm

    mappings:
        products:
            uri_prefix: '%app.path.pictures_directory%'
            upload_destination: '%kernel.project_dir%/public%app.path.pictures_directory%'
